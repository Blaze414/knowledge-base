# Playwright visual baselines are binary; keep line endings intact and
# opt-in to Git LFS if the repo enables it (safe no-op otherwise).
tests/visual/__screenshots__/**/*.png binary
tests/visual/__screenshots__/**/*.png filter=lfs diff=lfs merge=lfs -text
